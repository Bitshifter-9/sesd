generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Contact {
  id    Int    @id @default(autoincrement())
  name  String
  email String
  phone String
}
